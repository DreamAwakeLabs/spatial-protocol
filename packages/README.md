# DASP language bindings

Planned v0.1 bindings:

- `python/` — reference validator/client and simulator utilities
- `typescript/` — browser/operator/node client
- `cpp/` — portable C++ models/validation helpers usable by Unreal adapters

Bindings consume the canonical schemas and run the shared fixture corpus. They must not become independent sources of protocol truth.
